{
  "name": "club-dash",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/club-dash",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/next:build",
      "outputs": ["{options.outputPath}"],
      "defaultConfiguration": "production",
      "options": {
        "outputPath": "dist/apps/club-dash"
      },
      "configurations": {
        "development": {
          "outputPath": "apps/club-dash"
        },
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/next:server",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "club-dash:build",
        "dev": true
      },
      "configurations": {
        "development": {
          "buildTarget": "club-dash:build:development",
          "dev": true
        },
        "production": {
          "buildTarget": "club-dash:build:production",
          "dev": false
        }
      }
    },
    "export": {
      "executor": "@nx/next:export",
      "options": {
        "buildTarget": "club-dash:build:production"
      }
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": ["{workspaceRoot}/coverage/{projectRoot}"],
      "options": {
        "jestConfig": "apps/club-dash/jest.config.ts"
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": ["{options.outputFile}"],
      "options": {
        "lintFilePatterns": ["apps/club-dash/**/*.{ts,tsx,js,jsx}"]
      }
    }
  },
  "tags": []
}
