'use client';
import React from 'react';
// import { useCreateClubMutation } from '../../_hooks/use-create-club';
// import { useCreateClubState } from '../../_hooks/use-createclub-states';

export const ClubInfoComp = () => {
  //   const { state } = useCreateClubState();

  //   const { data } = useCreateClubMutation(state);
  return (
    <div>
      <div className="flex flex-col gap-8">info</div>
    </div>
  );
};
