import { UPDATE_TIMETABLE } from '../../app/_components/teacher/approved/timetable.mutations';

describe('timetable.mutations', () => {
  it('exports UPDATE_TIMETABLE mutation', () => {
    expect(UPDATE_TIMETABLE).toBeDefined();
  });
});
