overwrite: true
schema:
  - '${CLUB_SERVICE_ENDPOINT}'

documents:
  - './apps/club-dash/app/testForBackend/**/*.graphql'

generates:
  ./apps/club-dash/app/testForBackend/generated/:
    preset: client
    plugins: []
