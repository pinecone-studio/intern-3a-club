{
  "$schema": "http://json-schema.org/schema",
  "cli": "nx",
  "title": "Deploy Worker with Secrets",
  "description": "Deploy a Cloudflare Worker and upload secrets afterwards",
  "type": "object",
  "properties": {
    "environment": {
      "type": "string",
      "description": "The environment to deploy to (e.g., 'test', 'production')",
      "examples": ["test", "production", "dev"]
    },
    "workerName": {
      "type": "string",
      "description": "The name of the worker (optional, defaults to project name)"
    },
    "secretsFile": {
      "type": "string",
      "description": "Path to the secrets JSON file",
      "default": "secrets.json"
    }
  },
  "required": ["environment"]
}
